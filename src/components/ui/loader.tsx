import React from 'react';

const Loader = () => {
  // This component uses the `.loader` class defined in `src/app/globals.css`.
  return <div data-testid="loader" className="loader" />;
};

export default Loader;